# NEXUS GitHub upload

This folder is the extracted NEXUS project from the Bolt ZIP. It excludes generated `dist-server/` output and any `.env` secrets. Keep `.env.example` and create `.env` locally with your own keys.

Upload the contents of this folder to your GitHub repository root.
